

  <%= render 'shared/beta_request' %>

  <!-- <h2>Sign up</h2>
  <div class='well span5 offset3'>
  <p>
    You get free <%= Settings.invite_credit %> invite on signup today!
  </p>

  <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => 'form-horizontal'}) do |f| %>
      <%= f.error_notification %>

      <div class="inputs">
        <%= f.input :name, :required => true, :autofocus => true %>
        <%= f.input :email, :required => true %>
        <%= f.input :organization, :required => true %>
        <%= f.input :password, :required => true, :hint => 'atleast 6 characters' %>
        <%= f.input :password_confirmation, :required => true %>
        <%= f.input :role, :collection => %w[recruiter candidate], :include_blank => false %>
        <%= f.input :timezone, :as => :time_zone %>
      </div>

      <div class="actions">
        <%= f.button :submit, "Sign up" %>
      </div>
  <% end %>

  <%= render :partial => "devise/shared/links" %>
  </div> -->

