<%= link_to (t 'name'), root_path, :class => 'brand' %>
<ul class="nav" id="mainNavBar">
  <% if user_signed_in? %>
      <% if current_user.candidate? %>

      <% end %>
      <% if current_user.recruiter? %>
          <%= nav_link "Dashboard", dashboard_home_path %>
          <li class="divider-vertical"></li>
          <%= nav_link "Problems", problems_path %>
          <li class="divider-vertical"></li>
          <%= nav_link (t 'title.collection'), collections_path %>
          <li class="divider-vertical"></li>
          <%= nav_link "Invites", invites_path %>
          <!-- <li class="divider-vertical"></li>
          <%= nav_link "Interviews", interviews_path %> -->
      <% end %>
  <% else %>

  <% end %>
</ul>

<ul class="nav pull-right">
  <%= nav_link "FAQ", faq_path %>
  <li class="divider-vertical"></li>
  <%= nav_link "Contact", contact_path %>
  <li class="divider-vertical"></li>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      <%= user_signed_in? ? current_user.name : "Guest" %>
      <b class="caret"></b></a>
    <% if user_signed_in? %>
        <ul class="dropdown-menu">
          <li><%= link_to('Logout', destroy_user_session_path, :method => 'delete') %></li>
          <% unless current_user.demo_account? %>
              <li class="divider"></li>
              <li><%= link_to('Edit account', edit_user_registration_path) %></li>
          <% else %>
              <!-- <li class="divider"></li> -->
              <!-- <li><% link_to 'Register', demo_register_path %></li> -->
          <% end %>
        </ul>
    <% else %>
        <ul class="dropdown-menu" style="padding: 10px; padding-bottom: 3px;">
          <%= render :partial => 'shared/signin_form' %>
        </ul>
    <% end %>
  </li>
</ul>

